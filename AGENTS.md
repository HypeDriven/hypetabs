inspect spec.md for this project's specification
whenever changes are made to the project, make sure to clarify if the changes conflict with existing spec, and update the spec document whenever changes are made to keep it current
for greenfield backend services use c#
for greenfield frontend projects use typescript or wasm
ui should default to dark mode by default
prompt before adding third party dependencies so that supply chain injection attack surface area can be minimized
always make sure backend services are secure from malicious api users or malicious clients
never submit secrets, never submit .env files
when tools or workflows are identified, document them in WORKFLOWS.md for future sessions to benefit from the information
applications should always have an 'about' section in an appropriately discreet location that says 'Developed by hypedriven.com' that launches the https://www.hypedriven.com site when clicked
